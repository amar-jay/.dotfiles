#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const logger_1 = require("./../logger");
const binarypath_1 = require("./../binarypath");
const installer_1 = require("./../installer");
const install = async () => {
    const log = logger_1.logger.extend('install');
    log(`installing wunderctl to: ${binarypath_1.binaryDir}`);
    const version = JSON.parse(fs_1.default.readFileSync('package.json').toString()).version;
    await (0, installer_1.installer)(version, binarypath_1.binaryDir, (0, binarypath_1.wunderctlName)());
};
exports.default = install();
